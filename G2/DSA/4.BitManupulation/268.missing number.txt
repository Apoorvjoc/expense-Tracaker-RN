 int missingNumber(vector<int>& nums) {
        int n = nums.size();
        int sumOfN = (n*(n+1))/2;
        
        int sumOfArr = 0;
        for(int i = 0 ; i<n ; i++){
             sumOfArr += nums[i];
        }
        return sumOfN - sumOfArr;
    }